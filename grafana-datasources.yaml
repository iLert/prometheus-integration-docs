apiVersion: 1

datasources:
  - name: "prometheus"
    type: "prometheus"
    uid: prometheus
    orgId: 1
    version: 1
    isDefault: true
    editable: false
    access: "proxy"
    url: "http://prometheus:9090"
